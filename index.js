import { AppRegistry } from 'react-native';
import CameraScreen from './CameraScreen'; // Change this
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => CameraScreen); // Change this
